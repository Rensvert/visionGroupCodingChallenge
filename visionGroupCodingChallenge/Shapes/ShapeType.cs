namespace visionGroupCodingChallenge.Shapes
{
    // Enum for the different shapes
    public enum ShapeType
    {
        Square,
        Ellipse,
        Circle,
        Triangle,
        Polygon

    }
}
